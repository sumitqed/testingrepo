# Note that the example .travis.yml file for child projects lives in /install.
sudo: false
language: php
dist: trusty

php:
  - 7.1

matrix:
  fast_finish: true

jdk:
  - oraclejdk8

before_install:
  - php -v
  - 'cd abc; composer install;'

install:
  - echo "running install.";

script:
  - echo "running script";

deploy:
   - provider: script
     script: "echo"
     skip_cleanup: true
     on:
       branch: master
       php: 7.1
